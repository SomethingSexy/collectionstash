Change Database table: comment_types to entity_types

Rename comment_type_id to entity_type_id in comments and latest_comments

User Table add comment_count

EntityType add comment_count

Collectible add entity_type_id column, needs to be index

Stash add entity_type_id column, needs to be index

Remove type_id from entity_type, after I turn comment_Types into entity_types and AFTER I run the command to update stashes